server:
  port: 8082

spring:
  application:
    name: auth
  
  datasource:
    url: jdbc::mysql://localhost:3306/dwb_20262
    username: root
    password: EstaNoLaSubas
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Swagger
springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: method
    doc-expansion: none
